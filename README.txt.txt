1) Import schema from DB folder
2) Start MySQL server (ie via XAMPP)
3) run "node server.js" in this folder
4) Goto localhost:8080

*yelp folder contains custom script written to pull restaurant info into database (requires api key)